import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hide the on-screen dev tools indicator in local development (errors still show). */
  devIndicators: false,
};

export default nextConfig;
