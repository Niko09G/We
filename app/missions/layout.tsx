import type { ReactNode } from 'react'

/** Missions routes inherit Montserrat from root layout. */
export default function MissionsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
